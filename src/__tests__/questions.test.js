// import request from 'supertest'
// import app from '../app'

//Test for GET request for comments by question ID
describe('Routes and controllers for GET requests to /questions', () => {
/*
    test('Should return comments by questionid', () => {
        return request(app).get('/questions/?questionid=1234&comments=true').then(response => {
            // expect(response.body).toBe('These are comments by question id')
        })
    });

    test('Should return question by questionid', () => {
        return request(app).get('/questions/?questionid=1234').then(response => {
            // expect(response.body).toBe('This is a question by question id')
        })
    });

    test('Should return answers by questionid', () => {
        return request(app).get('/questions/?questionid=1234&answers=true').then(response => {
            // expect(response.body).toBe('These are answers by question id')
        })
    });

    test('Should return questions by userid', () => {
        return request(app).get('/questions/?userid=1234').then(response => {
            // expect(response.body).toBe('These are questions by user id')
        })
    });

    test('Should return all questions', () => {
        return request(app).get('/questions').then(response => {
            // expect(response.body).toBe('These are all questions')
        })
    });
*/
})


